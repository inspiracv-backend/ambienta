{
  "name": "ambienta",
  "version": "0.0.1",
  "private": true,
  "packageManager": "npm@10.8.2",
  "scripts": {
    "dev": "docker compose up",
    "dev:detached": "docker compose up -d",
    "dev:down": "docker compose down",
    "dev:logs": "docker compose logs -f",
    "dev:web": "npm run dev --workspace @ambienta/web",
    "dev:api": "docker compose up api",
    "build": "npm run build --workspace @ambienta/web",
    "lint": "npm run lint --workspace @ambienta/web",
    "typecheck": "tsc --noEmit -p apps/web/tsconfig.json",
    "prod:up": "docker compose -f docker-compose.prod.yml up -d --build",
    "prod:down": "docker compose -f docker-compose.prod.yml down",
    "prod:logs": "docker compose -f docker-compose.prod.yml logs -f"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "turbo": "^2.10.5"
  }
}
